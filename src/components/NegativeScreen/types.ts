import { IAwesomeIconProps } from '#cmp/AwesomeIcon/types'
interface INegativeScreenProps {
  iconProps?: IAwesomeIconProps
  title: string
  subtitle: string
  afterComponent?: React.ReactNode
  bg?: string
}

export type { INegativeScreenProps }
